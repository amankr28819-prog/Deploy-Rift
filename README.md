# RIFT

Welcome to the **RIFT** project repository.

## Overview
This repository is connected to local development. Any changes made locally can be synced directly to GitHub.

## Quick Sync
To sync local changes to GitHub, run:
```powershell
.\sync.ps1 "Your commit message"
```
Or simply:
```powershell
.\sync.ps1
```
